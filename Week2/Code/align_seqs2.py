import sys
with open(str(sys.argv[1]),'r') as g: 
    seqs=[]
    for each in seqs:

        for lines in g:
            a = []
            a.append(lines) #. 
            seqs[lines]=a
print (seqs)
        
        #seq1 = a
        #print(seq1)